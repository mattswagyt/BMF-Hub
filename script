local Decimals = 4
local Clock = os.clock()
local ValueText = "Value Is Now :"

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/drillygzzly/Roblox-UI-Libs/main/1%20Tokyo%20Lib%20(FIXED)/Tokyo%20Lib%20Source.lua"))({
    cheatname = "BMF Hub",
})

library:init()

-- Main Window
local Window1 = library.NewWindow({
    title = "Script Hub | Universal Hub",
    size = UDim2.new(0, 510, 0.6, 6)
})

-- Tabs
local GamesTab = Window1:AddTab("  Games  ")
local ScriptsTab = Window1:AddTab("  Scripts  ")
local LocalPlayerTab = Window1:AddTab("  Local Player  ")
local ServerTab = Window1:AddTab("  Server  ")
local MiscTab = Window1:AddTab("  Misc  ")
local ChangelogTab = Window1:AddTab("  Changelog  ")
local DiscordTab = Window1:AddTab("  Join our Discord!  ")
local CreditsTab = Window1:AddTab("  Credits  ")

-- Settings Tab
local SettingsTab = library:CreateSettingsTab(Window1)

-- Games Tab
local GamesSection = GamesTab:AddSection("Game-Specific Scripts", 1)

GamesSection:AddButton({
    enabled = true,
    text = "Prison Life - Prizz Life",
    tooltip = "Executes Prizz Life for Prison Life",
    confirm = true,
    callback = function()
        print("Executed Prizz Life!")
        loadstring(game:HttpGet('https://raw.githubusercontent.com/elliexmln/PrizzLife/main/pladmin.lua'))()
    end
})

GamesSection:AddButton({
    enabled = true,
    text = "Prison Life - Fent Admin",
    tooltip = "Executes Fent for Prison Life",
    confirm = true,
    callback = function()
        print("Executed Fent Admin!")
        loadstring(game:HttpGet("https://pastebin.com/raw/QM8nxmCX"))()
    end
})

GamesSection:AddButton({
    enabled = true,
    text = "Prison Life - Local Player",
    tooltip = "Executes Local Player",
    confirm = true,
    callback = function()
        print("Executed Local Player!")
        loadstring(game:HttpGet("https://pastebin.com/raw/iZ64yzjE"))()
    end
})

GamesSection:AddButton({
    enabled = true,
    text = "Prison Life - FE Bypass GUI",
    tooltip = "FE Bypass GUI for Prison Life",
    confirm = true,
    callback = function()
        print("Executed FE Bypass!")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/g00lXploiter/g00lXploiter/main/Fe%20bypass", true))()
    end
})

-- Scripts Tab
local ScriptsSection = ScriptsTab:AddSection("General Scripts", 1)

ScriptsSection:AddButton({
    enabled = true,
    text = "Infinite Yield",
    tooltip = "Runs Infinite Yield",
    confirm = true,
    callback = function()
        print("Executed Infinite Yield!")
        loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
    end
})

ScriptsSection:AddButton({
    enabled = true,
    text = "Dark Dex",
    tooltip = "Runs Dark Dex",
    confirm = true,
    callback = function()
        print("Executed Dark Dex!")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))()
    end
})

ScriptsSection:AddButton({
    enabled = true,
    text = "Chat Bypass",
    tooltip = "Runs Chat Bypass script",
    confirm = true,
    callback = function()
        print("Executed Chat Bypass!")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/AlgariBot/lua/refs/heads/Lua-Script-Executor/LocalNeverPatchedBypass.txt"))()
    end
})

ScriptsSection:AddButton({
    enabled = true,
    text = "FE Trolling GUI",
    tooltip = "Runs FE Trolling GUI",
    confirm = true,
    callback = function()
        print("Executed FE Trolling GUI!")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/yofriendfromschool1/Sky-Hub/main/FE%20Trolling%20GUI.luau"))()
    end
})

-- Local Player Tab
local LocalPlayerSection = LocalPlayerTab:AddSection("Player Modifications", 1)

LocalPlayerSection:AddSlider({
    enabled = true,
    text = "WalkSpeed",
    tooltip = "Change your walkspeed",
    flag = "WalkSpeedSlider",
    min = 16,
    max = 300,
    increment = 1,
    callback = function(v)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = v
    end
})

LocalPlayerSection:AddSlider({
    enabled = true,
    text = "JumpPower",
    tooltip = "Change your jump power",
    flag = "JumpPowerSlider",
    min = 50,
    max = 500,
    increment = 10,
    callback = function(v)
        game.Players.LocalPlayer.Character.Humanoid.JumpPower = v
    end
})

-- Server Tab
local ServerSection = ServerTab:AddSection("Server Actions", 1)

-- Rejoin Logic
ServerSection:AddButton({
    enabled = true,
    text = "Rejoin",
    tooltip = "Rejoin the current server",
    confirm = true,
    callback = function()
        local player = game.Players.LocalPlayer
        if player then
            game:GetService("TeleportService"):Teleport(game.PlaceId, player)
        else
            library:SendNotification("Failed to rejoin: Player not found!", 3)
        end
    end
})

-- Server Hop Logic
ServerSection:AddButton({
    enabled = true,
    text = "Server Hop",
    tooltip = "Join a different server",
    confirm = true,
    callback = function()
        local HttpService = game:GetService("HttpService")
        local TeleportService = game:GetService("TeleportService")
        local PlaceId = game.PlaceId

        -- Fetch a list of servers from Roblox's API
        local servers = {}
        local cursor = ""
        local success, errorMessage = pcall(function()
            repeat
                local response = HttpService:JSONDecode(game:HttpGet(
                    "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100&cursor=" .. cursor
                ))
                for _, server in pairs(response.data) do
                    if server.playing < server.maxPlayers and server.id ~= game.JobId then
                        table.insert(servers, server.id)
                    end
                end
                cursor = response.nextPageCursor
            until not cursor
        end)

        if not success then
            library:SendNotification("Failed to fetch servers: " .. errorMessage, 3)
            return
        end

        -- Attempt to hop to a new server
        if #servers > 0 then
            local randomServerId = servers[math.random(1, #servers)]
            TeleportService:TeleportToPlaceInstance(PlaceId, randomServerId)
        else
            library:SendNotification("No other servers available to hop to!", 3)
        end
    end
})

-- Misc Tab
local MiscSection = MiscTab:AddSection("Miscellaneous Features", 1)

MiscSection:AddToggle({
    text = "Enable Infinite Jump",
    state = false,
    flag = "InfiniteJump",
    callback = function(v)
        if v then
            print("Infinite Jump Enabled")
        else
            print("Infinite Jump Disabled")
        end
    end
})

-- Changelog Tab
local ChangelogSection = ChangelogTab:AddSection("Updates", 1)

ChangelogSection:AddLabel("Version 1.0 - This is the new version of BMF Hub!")
ChangelogSection:AddLabel("Version 1.0 - We had to remake our entire script hub, due to Orion Library getting hacked...")
ChangelogSection:AddLabel("Version 1.0 - We apologize!")
ChangelogSection:AddLabel("Version 1.0 - We'll slowly add back all of the scripts, as we test them first.")

-- Join our Discord Tab
local DiscordSection = DiscordTab:AddSection("Join Our Community", 1)

DiscordSection:AddButton({
    enabled = true,
    text = "Join Discord",
    tooltip = "Click to copy the invite link",
    callback = function()
        setclipboard("https://discord.gg/Prg2MNanTY") 
        library:SendNotification("Discord invite copied to clipboard!", 3)
    end
})

-- Credits Tab
local CreditsSection = CreditsTab:AddSection("Developer Credits", 1)

CreditsSection:AddLabel("Made by: BMF")
CreditsSection:AddLabel("Special Thanks: UnlimitedSkys")

-- Notification
local Time = (string.format("%."..tostring(Decimals).."f", os.clock() - Clock))
library:SendNotification(("Loaded In "..tostring(Time)), 6)

-- Done
